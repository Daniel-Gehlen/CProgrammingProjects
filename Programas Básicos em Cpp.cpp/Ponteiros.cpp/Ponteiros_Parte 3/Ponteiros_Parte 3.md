``` mermaid
graph TD
    A[Início] --> B[Declaração e inicialização das variáveis x, y e z]
    B --> C[Declaração dos ponteiros pX, pY e pZ]
    C --> D[Atribuição dos endereços de memória das variáveis aos ponteiros]
    D --> E[Impressão dos endereços e valores das variáveis usando os ponteiros]
    E --> F[Cálculo da soma de x e y usando os valores apontados pelos ponteiros]
    F --> G[Atribuição manual do endereço de memória da variável x ao ponteiro resultado]
    G --> H[Impressão do valor do endereço de x usando o ponteiro resultado]
    H --> I[Atribuição manual do endereço de memória da variável y ao ponteiro r0x61fee0]
    I --> J[Impressão do valor apontado pelo ponteiro r0x61fee0]
    J --> K[Fim]
```